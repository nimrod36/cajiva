name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        ruby-version: [ '2.7', '3.0', '3.1' ]
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: ${{ matrix.ruby-version }}
        bundler-cache: true
    
    - name: Run RSpec tests
      run: bundle exec rspec
    
    - name: Run Cucumber BDD tests with strict undefined
      run: bundle exec cucumber specs/ --strict-undefined --format progress
